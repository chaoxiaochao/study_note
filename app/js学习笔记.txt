1. 不要再‘块’里面声明变量，要在代码的一开始就声明好了。以避免发生歧义。
例如：
var i;
for(i; i<10; i++){
	
};
console.log(i);   //10
2. 我们在声明变量时，全局代码要在代码前端声明，函数中要在函数体一开始就声明好。除了这两个地方，其他地方都不要出现变量声明。而且建议用“单var”形式。
var 
	a = 10,
	b = 11,
	c = function(){};

3. 事件委托
对事件处理程序过多问题的解决方案就是‘事件委托’。键盘和鼠标事件
事件冒泡。
4. 